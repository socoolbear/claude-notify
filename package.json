{
  "name": "claude-notify",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/index.ts --target=node --outfile=dist/claude-notify.mjs --banner \"#!/usr/bin/env node\"",
    "build:binary": "bun build ./src/index.ts --compile --outfile=dist/bin/claude-notify && rm -f .*.bun-build",
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "lint": "biome check ./src",
    "fmt": "biome format --write ./src"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "latest"
  }
}
